import classes from './Title.module.css';
const Title=()=>{
    return ( 
    <div className={classes.title}>
      <p>Get to know about the Theatre, then Book Your Tickets</p>
    </div> );
}

export default Title;